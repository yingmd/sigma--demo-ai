# Mobile E-commerce Frontend Project

这是一个基于 React + Vite + TypeScript 开发的现代化移动端电商网站前端项目。项目完整复刻了 Figma 设计稿，采用响应式布局，完美适配各类移动设备，并包含完整的购物流程交互。

## 🌟 项目亮点

*   **1:1 高保真还原**：严格遵循 Figma 设计规范，精确还原视觉细节。
*   **全屏响应式设计**：基于 Flexible + REM 方案，适配不同尺寸的移动设备，支持 iPhone X+ 安全区域。
*   **现代化技术栈**：使用 React 18, Vite, TypeScript, SCSS Modules 等前沿技术。
*   **沉浸式交互体验**：吸顶搜索栏、丝滑的横向滚动、底部导航切换、点击反馈等。
*   **组件化架构**：高复用性的组件设计（如 TabBar, ProductCard），代码结构清晰易维护。
*   **Mock 数据驱动**：内置完整的数据模拟层，支持脱离后端独立运行和演示。

## 📱 核心功能模块

### 1. 首页 (Home)
*   **吸顶搜索栏**：页面滚动时自动吸附顶部，随时可用的搜索入口。
*   **快捷功能区**：收藏、历史、关注、订单等常用功能入口。
*   **营销 Banner**：展示最新促销活动，支持分页指示器。
*   **分类导航**：横向滚动的分类列表，支持点击筛选。
*   **热门商品流**：双列瀑布流展示推荐商品。

### 2. 搜索页 (Search)
*   **实时搜索**：输入关键词即时过滤商品（基于本地数据）。
*   **历史记录**：展示最近搜索关键词，支持快速回填。
*   **空状态处理**：友好的无结果提示。

### 3. 商品详情页 (Product Detail)
*   **沉浸式展示**：顶部大图展示，支持返回和分享操作。
*   **信息面板**：展示品牌、名称、价格、评分及详细描述。
*   **底部操作栏**：固定的"加入购物车"和"立即购买"按钮。

### 4. 购物车 (Cart)
*   **商品管理**：展示已选商品，支持数量增减和删除。
*   **价格计算**：实时自动计算订单总额。
*   **空状态**：购物车为空时的引导提示。

### 5. 个人中心 (Profile)
*   **用户信息**：展示头像、昵称及关键数据（订单数、关注数等）。
*   **功能菜单**：列表式导航，涵盖订单管理、设置、帮助等功能。

## 🛠 技术架构

### 目录结构
```
src/
├── assets/             # 静态资源（图片、SVG）
├── components/         # 公共组件
│   ├── common/         # 通用基础组件
│   └── layout/         # 布局组件 (TabBar等)
├── data/               # Mock 数据源 (mock.ts)
├── lib/                # 工具库 (utils.ts)
├── pages/              # 页面组件
│   ├── Cart/           # 购物车页
│   ├── ProductDetail/  # 商品详情页
│   ├── Profile/        # 个人中心页
│   ├── Category.tsx    # 分类页
│   ├── Home.tsx        # 首页
│   └── Search.tsx      # 搜索页
├── styles/             # 全局样式
│   ├── global.scss     # 全局重置与基础样式
│   ├── mixins.scss     # SCSS 混合宏
│   └── variables.scss  # 设计变量 (颜色、字体等)
├── App.tsx             # 路由配置
└── main.tsx            # 入口文件
```

### 关键技术点
*   **构建工具**: Vite - 极速的开发服务器和构建性能。
*   **路由管理**: React Router v6 - 声明式的路由配置与导航。
*   **样式方案**: SCSS Modules - 局部作用域 CSS，配合全局变量管理主题。
*   **CSS 工具**: `clsx` + `tailwind-merge` - 灵活高效的类名合并工具。
*   **图标库**: `react-icons` - 轻量级、按需加载的图标集合。

## 🚀 快速开始

### 环境要求
*   Node.js >= 16
*   npm 或 yarn

### 安装步骤

1.  **克隆项目**
    ```bash
    git clone <repository-url>
    cd figma-demo7
    ```

2.  **安装依赖**
    ```bash
    npm install
    ```

3.  **启动开发服务器**
    ```bash
    npm run dev
    ```
    访问 `http://localhost:5173` 查看效果。

4.  **构建生产版本**
    ```bash
    npm run build
    ```
    构建产物将输出到 `dist` 目录。

## 📚 开发指南

### 新增页面
1.  在 `src/pages` 下创建新目录或文件。
2.  使用 SCSS Modules 编写样式 (`ComponentName.module.scss`)。
3.  在 `src/App.tsx` 中注册路由。

### 修改数据
所有模拟数据均存储在 `src/data/mock.ts` 中，修改该文件即可实时更新全站数据展示。

### 样式定制
在 `src/styles/variables.scss` 中修改 CSS 变量即可调整全局主题色、字体大小等设计规范。

## 📄 沉淀技能 (Skills)

本项目开发过程中沉淀了以下可复用的 Trae Skills，位于 `.trae/skills` 目录：
*   **figma-to-code**: Figma 设计稿转前端代码指南。
*   **mobile-responsive-design**: 移动端 H5 响应式开发方案。
*   **react-component-architect**: 标准化 React 组件架构生成器。

---
Generated by Trae AI
